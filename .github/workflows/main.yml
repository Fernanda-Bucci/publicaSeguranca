name: CI-CD
on:
  push: 
    branches: ["main"]
  workflow_dispatch:

jobs: 
  CI:
    runs-on: ubuntu-latest
    steps: 
        - name: Build and push Docker images
          uses: docker/build-push-action@v6.9.0
          with:
            context: .
            file: ./Dockerfile
            push: true
            tags: |
              fernandabucci1910/publicaSeguranca: ${{ gihub.run_number }}
              fernandabucci1910/publicaSeguranca:latest
              
        - name: Passo 02
          run: echo "Publicar a imagem no Docker hub"
